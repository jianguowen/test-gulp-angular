export class RegisterService {
  constructor($http, $window, $state, toastr) {
    'ngInject';

    this.$http = $http;
    this.$state = $state;
    this.$window = $window;
    this.toastr = toastr;
  }

  //手机号码验证及发送验证码

  // 用户名验证

}
